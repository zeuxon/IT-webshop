import { StockstatusPipe } from './stockstatus.pipe';

describe('StockstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new StockstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
